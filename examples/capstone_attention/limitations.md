# 局限与下一步

- 本实验使用随机 Q、K，不包含学习后的语义结构。
- 熵和最大权重是分布统计，不是任务准确率或事实正确率。
- 只研究标准点积 Attention，未比较 RoPE、GQA、稀疏 Attention 或 FlashAttention 实现。
- CPU 浮点结果会随 PyTorch 版本略有变化，因此验收趋势而非固定小数。
- 下一步可固定训练任务，比较移除缩放前后的 loss、梯度范数和收敛速度；每次仍只改变一个主要变量。
